# Install note

Copy the entire `docs/framework_visual_orientation/` folder into the root of `a10-core-foundation`.

Then open:

```text
docs/framework_visual_orientation/index.html
```

This is a standalone browser file. No package installation is needed.
